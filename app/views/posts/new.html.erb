
<% title "New Tweet" %>
<%= bootstrap_form_with(model: @post, local: true) do |f| %>
    <%= f.text_area :content, hide_label: true, placeholder: "Type your tweet." %>
    <%= f.submit "Submit" %>
<% end %>

